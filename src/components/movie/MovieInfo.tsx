import React from 'react';

const MovieInfo = () => {
    return (
        <div>
            MovieInfo
        {/*    todo contains label, description, badges*/}
        </div>
    );
};

export default MovieInfo;